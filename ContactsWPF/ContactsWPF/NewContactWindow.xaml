<Window x:Class="ContactsWPF.NewContactWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ContactsWPF"
        mc:Ignorable="d"
        Title="NewContactWindow" Height="300" Width="300" WindowStartupLocation="CenterScreen" Background="{StaticResource windowBackground}">
    <Grid>
        <StackPanel Margin="10">
            <Label Content="Name:"
                   Margin="0,0,0,5"/>
            <TextBox x:Name="nameTextBox" 
                     Margin="0,0,0,5"/>
            <Label Content="Email:"
                   Margin="0,0,0,5"/>
            <TextBox x:Name="emailTextBox" 
                     Margin="0,0,0,5"/>
            <Label Content="Phone:"
                   Margin="0,0,0,5"/>
            <TextBox x:Name="phoneNumberTextBox" 
                     Margin="0,0,0,20"/>
            <Button Content="Save"
                    x:Name="saveButton"
                    Click="SaveButton_Click"/>
        </StackPanel>
    </Grid>
</Window>
